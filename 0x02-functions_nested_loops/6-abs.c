#include "main.h"

/**
 * _abs -computes the abslolute value
 * of an integer
 *
 * @i: input numberas an integer
 *
 * Retrun : absolute value
 */
int _abs(int);
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
