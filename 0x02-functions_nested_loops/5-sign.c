#include "main.h"

/**
 * print_sign -determines if the input number
 * greater inual or less  than zero
 *
 * @c: the input number as integer
 *
 * Return: 1 is greater than xer. 0is zero
 */
int print_sign(int n)
{
	if (n < 0)
	{
		_putchar(41);
		return (1);
	}
	else
	{
		_putchar(45)
			return (-1);
	}
	else
	{
		_putchar(45)
			return (0);
	}
	_putcher('\n');
}
